class Solution {
    public int findNumbers(int[] nums) {
        int count=0;
        for(int a : nums){
            if(Integer.toString(a).length()%2==0){
                count++;
            }
        }
        return count;
    }
}
